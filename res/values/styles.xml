<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="LoginFormContainer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">16dp</item>
    </style><!-- style for the tabs -->

	<style name="WindowTitleBackground" parent="android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@drawable/title_bar</item>
 	</style>
    
    <style name="WorkspaceActionBarTabStyle" parent="android:style/Widget.Holo.Light.Tab">
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:background">000</item>
    </style>
    
    <style name="Theme.Workspace" parent="android:style/Theme.Holo.Light">
        <item name="android:actionBarTabStyle">@style/WorkspaceActionBarTabStyle</item>
        <item name="android:actionBarStyle">@style/WindowTitleBackground</item>
        <!--  <item name="android:actionBarTabStyle">@style/tab_nav</item>
        <item name="android:actionBarTabTextStyle">@style/tab_nav_text</item> -->
        <item name="android:actionBarSize">50dp</item>
        <item name="android:showAsAction">always</item>
    </style>
</resources>