<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

<Button
    android:id="@+id/plant_change_button"
    android:layout_width="fill_parent"
    android:layout_height="100dp"
    android:visibility="gone" >

</Button>
    
       <ImageView
           android:id="@+id/plant_thumbnail"
           android:layout_width="80dp"
           android:layout_height="80dp"
           android:layout_alignParentTop="true"
           android:layout_marginLeft="15dp"
           android:layout_marginTop="10dp"
           android:background="@color/main_theme_color"
           android:minHeight="100dp"
           android:minWidth="100dp"
           android:scaleType="centerInside"
           android:visibility="gone" />

    <TextView
        android:id="@+id/common_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/plant_thumbnail"
        android:layout_alignTop="@+id/plant_thumbnail"
        android:layout_marginLeft="100dp"
        android:text="Select Plant"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:visibility="gone" />

    <TextView
        android:id="@+id/taxon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/common_name"
        android:layout_below="@+id/common_name"
        android:textAppearance="?android:attr/textAppearanceSmall" />

    <LinearLayout
        android:id="@+id/observation_fragment_container"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/plant_thumbnail"
        android:orientation="vertical"
         >

    </LinearLayout>

</RelativeLayout>